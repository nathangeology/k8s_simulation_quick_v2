apiVersion: apps/v1
kind: Deployment
metadata:
  name: parallel-query-scheduler
spec:
  replicas: 2
  selector:
    matchLabels:
      app: parallel-query-scheduler
  template:
    metadata:
      labels:
        app: parallel-query-scheduler
    spec:
      terminationGracePeriodSeconds: 30
      containers:
      - name: parallel-query-scheduler-container
        image: nginx:latest
        resources:
          requests:
            cpu: '2.0'
            memory: 600Mi
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
          - labelSelector:
              matchLabels:
                name: parallel-query-scheduler
            topologyKey: kubernetes.io/hostname
