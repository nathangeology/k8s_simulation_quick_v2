apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis-live-2
spec:
  replicas: 1
  selector:
    matchLabels:
      app: redis-live-2
  template:
    metadata:
      labels:
        app: redis-live-2
    spec:
      terminationGracePeriodSeconds: 30
      containers:
      - name: redis-live-2-container
        image: nginx:latest
        resources:
          requests:
            cpu: '3.1'
            memory: 8292Mi
      tolerations:
      - effect: NoSchedule
        key: type
        operator: Equal
        value: data-node
