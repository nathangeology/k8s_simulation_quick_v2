apiVersion: apps/v1
kind: Deployment
metadata:
  name: canary-exporter
spec:
  replicas: 1
  selector:
    matchLabels:
      app: canary-exporter
  template:
    metadata:
      labels:
        app: canary-exporter
    spec:
      terminationGracePeriodSeconds: 30
      containers:
      - name: canary-exporter-container
        image: nginx:latest
        resources:
          requests:
            cpu: '0.1'
            memory: 128Mi
