apiVersion: apps/v1
kind: Deployment
metadata:
  name: querier-burst
spec:
  replicas: 1
  selector:
    matchLabels:
      app: querier-burst
  template:
    metadata:
      labels:
        app: querier-burst
    spec:
      terminationGracePeriodSeconds: 30
      containers:
      - name: querier-burst-container
        image: nginx:latest
        resources:
          requests:
            cpu: '1.5'
            memory: 3072Mi
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: grafana.com/platform-spot-solo
                operator: Exists
      tolerations:
      - effect: NoSchedule
        key: type
        operator: Equal
        value: spot-node
