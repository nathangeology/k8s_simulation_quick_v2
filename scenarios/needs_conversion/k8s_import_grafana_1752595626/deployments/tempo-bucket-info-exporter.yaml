apiVersion: apps/v1
kind: Deployment
metadata:
  name: tempo-bucket-info-exporter
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tempo-bucket-info-exporter
  template:
    metadata:
      labels:
        app: tempo-bucket-info-exporter
    spec:
      terminationGracePeriodSeconds: 30
      containers:
      - name: tempo-bucket-info-exporter-container
        image: nginx:latest
        resources:
          requests:
            cpu: '0.1'
            memory: 128Mi
