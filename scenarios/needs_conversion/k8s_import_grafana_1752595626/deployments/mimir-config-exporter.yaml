apiVersion: apps/v1
kind: Deployment
metadata:
  name: mimir-config-exporter
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mimir-config-exporter
  template:
    metadata:
      labels:
        app: mimir-config-exporter
    spec:
      terminationGracePeriodSeconds: 30
      containers:
      - name: mimir-config-exporter-container
        image: nginx:latest
        resources:
          requests:
            cpu: '0.1'
            memory: 128Mi
